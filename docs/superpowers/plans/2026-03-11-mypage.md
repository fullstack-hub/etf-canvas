# 마이페이지 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자 프로필 관리(닉네임, 추가정보, 공개설정, 제3자 동의, 로그아웃/탈퇴) 마이페이지 구현

**Architecture:** Prisma에 User 모델 추가, NestJS user 모듈(GET/PATCH/DELETE /user/me), Next.js에 mypage 뷰 컴포넌트. 기존 Keycloak JWT sub을 keycloakId로 매핑. GET /user/me 호출 시 유저가 없으면 자동 생성(upsert).

**Tech Stack:** Prisma, NestJS, Next.js, shadcn/ui, React Query, Zustand

---

## File Structure

### API (apps/api)
| Action | Path | Purpose |
|--------|------|---------|
| Modify | `prisma/schema.prisma` | User 모델 추가, Portfolio에 relation 추가 |
| Create | `src/user/user.module.ts` | User 모듈 |
| Create | `src/user/user.controller.ts` | GET/PATCH/DELETE /user/me |
| Create | `src/user/user.service.ts` | 프로필 CRUD + 회원탈퇴 로직 |
| Modify | `src/app.module.ts` | UserModule import |
| Modify | `src/auth/jwt.guard.ts` | req에 provider 정보 추가 (idp 힌트) |

### Web (apps/web)
| Action | Path | Purpose |
|--------|------|---------|
| Create | `src/components/mypage-view.tsx` | 마이페이지 단일 스크롤 컴포넌트 |
| Modify | `src/lib/api.ts` | user API 함수 추가 |
| Modify | `src/lib/store.ts` | currentView에 'mypage' 추가 |
| Modify | `src/components/icon-sidebar.tsx` | 마이페이지 버튼 연결 |
| Modify | `src/components/login-modal.tsx` | 하단 동의 문구 추가 |
| Modify | `src/app/page.tsx` | mypage 뷰 렌더링 |

---

## Chunk 1: DB + API

### Task 1: Prisma User 모델 추가

**Files:**
- Modify: `apps/api/prisma/schema.prisma`

- [ ] **Step 1: schema.prisma에 User 모델 추가**

`schema.prisma` 맨 아래에 추가:

```prisma
model User {
  id                String    @id @default(uuid()) @db.Uuid
  keycloakId        String    @unique @map("keycloak_id")
  nickname          String?   @unique
  name              String?
  phone             String?
  age               String?
  gender            String?
  investExp         String?   @map("invest_exp")
  investStyle       String?   @map("invest_style")
  showAge           Boolean   @default(false) @map("show_age")
  showGender        Boolean   @default(false) @map("show_gender")
  showInvestExp     Boolean   @default(false) @map("show_invest_exp")
  showInvestStyle   Boolean   @default(false) @map("show_invest_style")
  thirdPartyConsent Boolean   @default(false) @map("third_party_consent")
  consentAt         DateTime? @map("consent_at")
  consentRevokedAt  DateTime? @map("consent_revoked_at")
  provider          String?
  createdAt         DateTime  @default(now()) @map("created_at")
  updatedAt         DateTime  @updatedAt @map("updated_at")

  @@map("users")
}
```

- [ ] **Step 2: 마이그레이션 SQL 파일 생성**

```bash
cd apps/api && npx prisma migrate dev --name add_users --create-only
```

- [ ] **Step 3: 생성된 SQL 파일 리뷰**

SQL 파일 내용 확인: `CREATE TABLE "users"` + 컬럼, unique constraint 확인.

- [ ] **Step 4: 마이그레이션 적용**

```bash
cd apps/api && npx prisma migrate deploy
```

- [ ] **Step 5: Prisma client 재생성**

```bash
cd apps/api && npx prisma generate
```

- [ ] **Step 6: 커밋**

```bash
git add apps/api/prisma/
git commit -m "feat: add User model to Prisma schema"
```

---

### Task 2: User 모듈 — Service

**Files:**
- Create: `apps/api/src/user/user.service.ts`

- [ ] **Step 1: user.service.ts 작성**

```typescript
import { Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class UserService {
  constructor(private readonly prisma: PrismaService) {}

  /** 프로필 조회 — 없으면 자동 생성 */
  async getOrCreate(keycloakId: string, provider?: string) {
    let user = await this.prisma.user.findUnique({ where: { keycloakId } });
    if (!user) {
      user = await this.prisma.user.create({
        data: { keycloakId, provider },
      });
    }
    return user;
  }

  /** 프로필 수정 */
  async update(keycloakId: string, data: {
    nickname?: string;
    name?: string;
    phone?: string;
    age?: string;
    gender?: string;
    investExp?: string;
    investStyle?: string;
    showAge?: boolean;
    showGender?: boolean;
    showInvestExp?: boolean;
    showInvestStyle?: boolean;
    thirdPartyConsent?: boolean;
  }) {
    const user = await this.prisma.user.findUnique({ where: { keycloakId } });
    if (!user) throw new NotFoundException();

    // 제3자 동의 변경 시 일시 기록
    const consentUpdate: Record<string, any> = {};
    if (data.thirdPartyConsent !== undefined && data.thirdPartyConsent !== user.thirdPartyConsent) {
      if (data.thirdPartyConsent) {
        consentUpdate.consentAt = new Date();
        consentUpdate.consentRevokedAt = null;
      } else {
        consentUpdate.consentRevokedAt = new Date();
      }
    }

    return this.prisma.user.update({
      where: { keycloakId },
      data: { ...data, ...consentUpdate },
    });
  }

  /** 회원탈퇴 — 포트폴리오 전체 삭제 + User 삭제 */
  async withdraw(keycloakId: string) {
    // 포트폴리오의 userId는 keycloakId (JWT sub)
    await this.prisma.portfolio.deleteMany({ where: { userId: keycloakId } });
    await this.prisma.user.delete({ where: { keycloakId } });
    return { ok: true };
  }
}
```

- [ ] **Step 2: 커밋**

```bash
git add apps/api/src/user/user.service.ts
git commit -m "feat: add UserService with getOrCreate, update, withdraw"
```

---

### Task 3: User 모듈 — Controller + Module

**Files:**
- Create: `apps/api/src/user/user.controller.ts`
- Create: `apps/api/src/user/user.module.ts`
- Modify: `apps/api/src/app.module.ts`

- [ ] **Step 1: user.controller.ts 작성**

```typescript
import { Controller, Get, Patch, Delete, Body, Req, UseGuards } from '@nestjs/common';
import { ApiBearerAuth } from '@nestjs/swagger';
import { UserService } from './user.service';
import { JwtGuard } from '../auth/jwt.guard';

@Controller('user')
@UseGuards(JwtGuard)
@ApiBearerAuth('jwt')
export class UserController {
  constructor(private readonly svc: UserService) {}

  @Get('me')
  getMe(@Req() req: any) {
    return this.svc.getOrCreate(req.userId, req.provider);
  }

  @Patch('me')
  updateMe(
    @Req() req: any,
    @Body() body: {
      nickname?: string;
      name?: string;
      phone?: string;
      age?: string;
      gender?: string;
      investExp?: string;
      investStyle?: string;
      showAge?: boolean;
      showGender?: boolean;
      showInvestExp?: boolean;
      showInvestStyle?: boolean;
      thirdPartyConsent?: boolean;
    },
  ) {
    return this.svc.update(req.userId, body);
  }

  @Delete('me')
  withdraw(@Req() req: any) {
    return this.svc.withdraw(req.userId);
  }
}
```

- [ ] **Step 2: user.module.ts 작성**

```typescript
import { Module } from '@nestjs/common';
import { PrismaModule } from '../prisma/prisma.module';
import { UserController } from './user.controller';
import { UserService } from './user.service';

@Module({
  imports: [PrismaModule],
  controllers: [UserController],
  providers: [UserService],
  exports: [UserService],
})
export class UserModule {}
```

- [ ] **Step 3: app.module.ts에 UserModule import 추가**

`apps/api/src/app.module.ts`의 imports 배열에 `UserModule` 추가.

```typescript
import { UserModule } from './user/user.module';

// imports 배열:
imports: [PrismaModule, RedisModule, NaverModule, EtfModule, PortfolioModule, UserModule],
```

- [ ] **Step 4: 빌드 확인**

```bash
pnpm turbo build --filter=@etf-canvas/api
```

- [ ] **Step 5: 커밋**

```bash
git add apps/api/src/user/ apps/api/src/app.module.ts
git commit -m "feat: add User controller, module, register in AppModule"
```

---

### Task 4: JwtGuard에 provider 정보 추가

**Files:**
- Modify: `apps/api/src/auth/jwt.guard.ts`

- [ ] **Step 1: JWT payload에서 identity_provider 추출**

Keycloak JWT에는 `identity_provider` 클레임이 포함됨 (kakao, naver, google). `jwt.guard.ts`에서 `req.provider`로 전달:

```typescript
// 기존: req.userId = payload.sub;
// 추가:
req.provider = (payload as any).identity_provider ?? null;
```

- [ ] **Step 2: 커밋**

```bash
git add apps/api/src/auth/jwt.guard.ts
git commit -m "feat: extract identity_provider from JWT in guard"
```

---

## Chunk 2: Frontend

### Task 5: API 클라이언트에 user 함수 추가

**Files:**
- Modify: `apps/web/src/lib/api.ts`

- [ ] **Step 1: api 객체에 user 관련 함수 추가**

`api.ts`의 `api` 객체 맨 아래에 추가:

```typescript
// User
getMe: () =>
  fetcher<{
    id: string;
    keycloakId: string;
    nickname: string | null;
    name: string | null;
    phone: string | null;
    age: string | null;
    gender: string | null;
    investExp: string | null;
    investStyle: string | null;
    showAge: boolean;
    showGender: boolean;
    showInvestExp: boolean;
    showInvestStyle: boolean;
    thirdPartyConsent: boolean;
    provider: string | null;
    createdAt: string;
  }>('/user/me'),

updateMe: (data: Record<string, any>) =>
  fetcher<any>('/user/me', {
    method: 'PATCH',
    body: JSON.stringify(data),
  }),

withdrawMe: () =>
  fetcher<{ ok: boolean }>('/user/me', { method: 'DELETE' }),
```

- [ ] **Step 2: 커밋**

```bash
git add apps/web/src/lib/api.ts
git commit -m "feat: add user API client functions"
```

---

### Task 6: Store에 mypage 뷰 추가

**Files:**
- Modify: `apps/web/src/lib/store.ts`

- [ ] **Step 1: currentView 타입에 'mypage' 추가**

`store.ts`에서 `'canvas' | 'portfolio' | 'gallery' | 'settings'` → `'canvas' | 'portfolio' | 'gallery' | 'settings' | 'mypage'` 로 변경. (interface 정의와 `setCurrentView` 타입 모두)

- [ ] **Step 2: 커밋**

```bash
git add apps/web/src/lib/store.ts
git commit -m "feat: add mypage to currentView type"
```

---

### Task 7: 마이페이지 컴포넌트 작성

**Files:**
- Create: `apps/web/src/components/mypage-view.tsx`

- [ ] **Step 1: mypage-view.tsx 작성**

단일 스크롤 페이지. 섹션 순서:
1. 프로필 헤더 (아바타 + 닉네임 + 연동 계정)
2. 닉네임 설정 (인라인 수정)
3. 추가정보 (선택) — 이름, 전화번호, 나이/성별, 투자경험/투자성향
4. 공개설정 — 4개 토글
5. 로그아웃 / 회원탈퇴
6. 제3자 정보제공 동의 (맨 아래, 눈에 덜 띄게)

핵심 구현 사항:
- `useQuery('user-me', api.getMe)` 로 프로필 로드
- 각 필드 변경 시 `api.updateMe()` 호출 (debounce 또는 저장 버튼)
- 회원탈퇴: AlertDialog로 확인 → `api.withdrawMe()` → Keycloak 로그아웃 → 홈 리다이렉트
- 로그아웃: 기존 icon-sidebar.tsx의 로직 재사용
- provider에 따라 카카오/네이버/구글 아이콘+텍스트 표시
- 드롭다운 선택지:
  - 나이: 10대, 20대, 30대, 40대, 50대, 60대 이상
  - 성별: 남성, 여성
  - 투자경험: 없음, 1년 미만, 1~3년, 3~5년, 5~10년, 10년 이상
  - 투자성향: 안정형, 안정추구형, 위험중립형, 적극투자형, 공격투자형

스타일: 기존 `settings-view.tsx` 패턴을 따름 (`max-w-2xl mx-auto px-6 py-8`).

- [ ] **Step 2: 커밋**

```bash
git add apps/web/src/components/mypage-view.tsx
git commit -m "feat: add MypageView component"
```

---

### Task 8: 라우팅 연결

**Files:**
- Modify: `apps/web/src/app/page.tsx`
- Modify: `apps/web/src/components/icon-sidebar.tsx`

- [ ] **Step 1: page.tsx에 MypageView 렌더링 추가**

`apps/web/src/app/page.tsx`에서 currentView 분기에 `'mypage'` 케이스 추가:

```typescript
import { MypageView } from '@/components/mypage-view';

// 기존 분기에 추가:
currentView === 'mypage' ? (
  <MypageView />
) : currentView === 'settings' ? (
```

- [ ] **Step 2: icon-sidebar.tsx에서 마이페이지 버튼 연결**

기존 `onClick={() => { setShowMenu(false); }}` (마이페이지 버튼, 111행)을:

```typescript
onClick={() => { setShowMenu(false); navigate('mypage'); }}
```

로 변경. `navigate` 함수의 타입에도 `'mypage'` 추가.

- [ ] **Step 3: 커밋**

```bash
git add apps/web/src/app/page.tsx apps/web/src/components/icon-sidebar.tsx
git commit -m "feat: wire mypage view into routing and sidebar"
```

---

### Task 9: 로그인 모달 동의 문구 추가

**Files:**
- Modify: `apps/web/src/components/login-modal.tsx`

- [ ] **Step 1: 소셜 로그인 버튼 아래에 동의 문구 추가**

`login-modal.tsx`에서 `</div>` (flex flex-col gap-2.5 닫는 태그) 뒤에:

```tsx
<p className="text-[11px] text-muted-foreground/60 text-center mt-4 leading-relaxed">
  계속하면{' '}
  <a href="/terms" target="_blank" className="underline hover:text-muted-foreground">이용약관</a>,{' '}
  <a href="/privacy" target="_blank" className="underline hover:text-muted-foreground">개인정보처리방침</a>,{' '}
  <a href="/privacy#third-party" target="_blank" className="underline hover:text-muted-foreground">제3자 정보제공</a>에
  동의한 것으로 간주합니다.
</p>
```

- [ ] **Step 2: 커밋**

```bash
git add apps/web/src/components/login-modal.tsx
git commit -m "feat: add consent text to login modal"
```

---

### Task 10: 빌드 확인 + 버전 bump

**Files:**
- Modify: `apps/api/helm/etf-canvas-api/Chart.yaml` (appVersion bump)
- Modify: `apps/web/helm/etf-canvas-web/Chart.yaml` (appVersion bump)

- [ ] **Step 1: API 빌드 확인**

```bash
pnpm turbo build --filter=@etf-canvas/api
```

- [ ] **Step 2: Web 빌드 확인**

```bash
pnpm turbo build --filter=web
```

- [ ] **Step 3: 양쪽 Chart.yaml appVersion bump**

API: 현재 버전 → +1 (예: 0.5.23 → 0.5.24)
Web: 현재 버전 → +1 (예: 0.5.19 → 0.5.20)

- [ ] **Step 4: 최종 커밋**

```bash
git add .
git commit -m "feat: 마이페이지 — User 모델 + 프로필 API + 마이페이지 UI + 로그인 모달 동의 문구"
```
